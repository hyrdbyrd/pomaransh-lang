#include <stdio.h>
#include <string.h>
#include "vm.h"

// Define boolean type
typedef unsigned char bool;

const char** operators = { "+", "=", "+=", "-=", "-" };

/** @access AST tree */
int parse(char* str) {

}

int main(char* args) {
    return 0;
}
